import React from 'react';
import Input from './Input/Input'
import SearchButton from './SearchButton/SearchButton'

const SearchContainer = () => {
    return (
        <div>
            here will be:
            <Input/>
            input,
            checkboxes for search category
            <SearchButton/>
            
        </div>
    );
};

export default SearchContainer;